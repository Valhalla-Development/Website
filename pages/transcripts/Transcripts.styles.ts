import { createStyles } from '@mantine/core';

export default createStyles(() => ({
    container: {
        backgroundColor: '#36393e',
        height: '100vh',
    },

    iframe: {
        width: '100%',
        height: '100%',
        border: 'none',
    },
}));
